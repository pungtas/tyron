import { InitTyron } from "../../../blockchain/tyronzil";
import { NetworkNamespace } from '../../tyronZIL-schemes/did-scheme';
export default class Resolver {
    static validateAvatar(avatar: string): Promise<void>;
    static resolveDns(network: NetworkNamespace, initTyron: InitTyron, domainName: string): Promise<string>;
}
